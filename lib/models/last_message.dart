class LastMessage {
  final String senderId;
  final String recieverId;
  final String lastMessage;
  final DateTime timeSent;

  LastMessage(this.senderId, this.recieverId, this.lastMessage, this.timeSent);
}
